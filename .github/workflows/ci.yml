# CI mirrors the Stop hook: ./verify fast, nothing more. Economical per
# autonomous Decision 31 — PR + push-to-master, superseded runs cancelled,
# docs-only changes skipped. The leak_gate and ip_gate run here on a bare
# runner (no venv), so they block a merge even when tests are skipped.
name: ci
on:
  push:
    branches: [master]
    paths-ignore: ["**.md", "!deploy/README.md", "!CLAUDE.md"]   # docs carried the leaks — keep gating them
  pull_request:
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - name: venv + deps (so tests run, not just the gates)
        run: |
          python -m venv venv
          # No mcp pin: mcp_server.py now imports the 2.x server class with a
          # 1.x fallback (autonomous-lifeos-001 addendum → migrated), so a
          # fresh install on the current SDK works. CI runs whatever
          # requirements.txt resolves — deliberately, so a future SDK break
          # shows up HERE first rather than on a fresh clone.
          venv/bin/pip install -r requirements.txt pytest
      - name: ./verify fast
        run: |
          chmod +x verify
          ./verify fast
